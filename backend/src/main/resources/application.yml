telegram:
  token: ${TG_BOT_TOKEN}
  username: ${TG_BOT_USERNAME}


server:
  port: 8080

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:}

google:
  sheets:
    api-key: ${GOOGLE_SHEETS_API_KEY}
    spreadsheet-id: 1Lwk-XBSU32uO6lxfq2jR8BFKrFlUWGEaK32NOu8Prbk

spring:
  config:
    import: classpath:cities.yml
  application:
    name: transfer
  datasource:
    password: ${POSTGRES_PASSWORD:postgres}
    username: ${POSTGRES_USER:postgres}
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/transfer_db
    driver-class-name: org.postgresql.Driver